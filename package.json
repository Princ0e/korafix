{
  "name": "korafix-root",
  "version": "1.0.0",
  "private": true,
  "description": "KoraFix platform root package configuration for cloud deployment (Render / Vercel)",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd server && node server.js"
  }
}
